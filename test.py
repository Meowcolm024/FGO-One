from util.split import split
from extra.matching import matching
"""
split()
out = get_extra()
for i in range(len(out)):
    print(out[i].order, out[i].count)
"""
split()
cards = matching()
for i in range(5):
    print(cards[i].servant, cards[i].chain, cards[i].crd, cards[i].mark)
